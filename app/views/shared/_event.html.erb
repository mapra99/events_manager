<% events_group.each do |event| %>
    <article>
      <h3 style="display: inline-block"> <%= link_to event.title, event %> </h3>
      <span style="display: inline-block"> <%= event.location %>, <%= event.date %> </span>
    </article>
<% end %>